- **Telegram**: Configurado y funcionando con nuevo token, sin conflictos. Ajustado para respuestas concisas sin razonamiento.
- **Freshservice**: Skill configurado, API Key guardada en `.freshservice.env`. Soporte para `list-tickets`, `get-ticket`, `create-ticket` (con timesheet y cierre automático), `list-time-entries`, `create-time-entry`, `update-ticket`, `reply-ticket`, `add-note`.
- **Paperclip**: Agente `OpenClaw FreshService Integrator` (`50260ba5-d37d-4c3f-ba7e-9805666591aa`) creado con rol `general` y `adapterType: http`. API Key (`pcp_b47f8282b216f66ec3f9ff62f6033267e04d17172fcb0e00`) guardada en `.paperclip.env`. El `adapterConfig.url` para conectar Paperclip a OpenClaw (`http://127.0.0.1:18789/`) está pendiente de configurar directamente en la DB de Supabase de Paperclip.
- **Paperclip Connector Skill (`paperclip-connector`)**: Creado con `create-issue` para enviar tareas a Paperclip, incluyendo `companyId` (`51d99c93-2cbc-4a98-b92d-b2b6ba75d877`). 
- **Company Docs Skill (`company-docs`)**: Creado para documentar información de empresas en `memory/empresas/`. Se probó creando un documento para `Soporte easyTEK` y se creó un issue en Paperclip con éxito. 
- **WhatsApp**: Enlazado (`+50671163399`) y configurando `dmPolicy: allowlist` con `allowFrom` (`+50683288460`) para iniciar el canal.
- **LLM**: Usando `google/gemini-2.5-flash` como principal y `openai-codex/gpt-5.4` como fallback. Los `rate limit` y `insufficient_quota` venían de `memory embeddings` de OpenAI. Pendiente verificar la cuota de OpenAI para embeddings o configurar otro proveedor.
- **Lanzadores de escritorio**: Creados para Paperclip y OpenClaw.
- **Problemas de sintaxis Python/Zsh**: Hubo problemas recurrentes con el `zsh -n` al validar los scripts Python debido a la interpretación de type hints, pero los scripts deberían ejecutarse correctamente con Python 3.
- **Repositorio easytek_itsm**: Clonado en `/tmp/easytek_itsm_repo` y revisados `OPENCLAW_INTEGRATION.md` y `OPENCLAW_QUICKSTART.md` para contexto de integración.